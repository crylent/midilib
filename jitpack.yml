jdk:
  - openjdk20
before_install:
  - sdk install java 20.0.2
  - sdk use java 20.0.2
  - sdk install maven
  - yes | sdkmanager "cmake;3.22.1"
install:
  - mvn clean package
  - mvn install -Dmaven.javadoc.skip=true -DskipTests
  - ./gradlew clean install -xtest